<div class="row mt-4">
  <div class="col-md-6">
    <h3>Search for Books:</h3>
    {{! Add other book search fields }}
    <form class="form new-book-form">
      <div class="form-group">
        <label for="book-name">Title:</label>
        <input class="form-input" type="text" id="book-name" name="book-name" />
      </div>
      <div class="form-group">
        <label for="author">Author:</label>
        <textarea class="form-input" id="author" name="author"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Find</button>
      </div>
    </form>
  </div>

{{!-- Conditionally render the results of the open library api here --}}
<section class="row">
  <div class="col-md-6">
    <h3>Results:</h3>
    {{!-- Add a conditional here to check if there are any results --}}
    {{if results.length > 0}}
    {{!-- If there are results, loop over them and render them --}}
    {{!-- If there are no results, render a message --}}
  </div>